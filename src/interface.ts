// interface
interface IHero{
    name: string,
    age: number
}

type Hero={
    name: string,
    age: number
}

// type HeroFunc = (name: string, age:number) => string
// type action = string
// type alias => object, function, array

const HeroInfo:Hero = {
    name: "khan",
    age: 42
}